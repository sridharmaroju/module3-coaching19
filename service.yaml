apiVersion: v1
kind: Service
metadata:
  name: sridhar-echo-service
  namespace: sridhar-eks-activity
spec:
  selector:
    app: echo-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5678